# 快速開始指南

## 🚀 立即開始使用

### 1️⃣ 啟動本地伺服器

**方法一: 使用啟動腳本 (推薦)**
```powershell
.\start-server.ps1
```

**方法二: 手動啟動**
```powershell
hugo server -D
```

然後在瀏覽器中訪問: `http://localhost:1313`

### 2️⃣ 建立新文章

```powershell
hugo new posts/我的第一篇文章.md
```

文章會建立在 `content/posts/` 目錄下。

### 3️⃣ 編輯文章

使用任何文字編輯器打開文章,例如:
- VS Code
- Notepad++
- 記事本

文章格式:
```markdown
---
title: "文章標題"
date: 2025-11-20T16:00:00+08:00
draft: false
tags: ["標籤1", "標籤2"]
categories: ["分類"]
---

在這裡寫你的內容...

## 標題

內容...
```

### 4️⃣ 發布文章

1. 確認 `draft: false`
2. 儲存文件
3. Hugo 會自動重新建置,重新整理瀏覽器即可看到

## 📝 常用命令

### 建立內容
```powershell
# 建立文章
hugo new posts/文章名稱.md

# 建立頁面
hugo new about.md
```

### 本地開發
```powershell
# 啟動伺服器 (包含草稿)
hugo server -D

# 啟動伺服器 (不包含草稿)
hugo server

# 指定端口
hugo server -D --port 8080
```

### 建置網站
```powershell
# 建置生產版本
hugo

# 建置並最小化
hugo --minify

# 清理並建置
hugo --gc --minify
```

## 🎨 自訂設定

### 修改網站資訊

編輯 `hugo.toml`:

```toml
title = '你的部落格名稱'
languageCode = 'zh-tw'

[params]
  description = "你的部落格描述"
  author = "你的名字"
```

### 修改選單

在 `hugo.toml` 中找到 `[[menu.main]]` 區塊進行修改。

### 添加社交連結

```toml
[[params.socialIcons]]
  name = "github"
  url = "https://github.com/你的用戶名"

[[params.socialIcons]]
  name = "email"
  url = "mailto:your.email@example.com"
```

## 🔍 功能使用

### 搜尋功能
- 訪問 `/search/` 頁面
- 輸入關鍵字搜尋文章

### 標籤系統
- 在文章 Front Matter 中添加 `tags: ["標籤1", "標籤2"]`
- 訪問 `/tags/` 查看所有標籤

### 分類系統
- 在文章 Front Matter 中添加 `categories: ["分類"]`
- 訪問 `/categories/` 查看所有分類

### 歸檔
- 訪問 `/archives/` 查看所有文章的時間軸

## 📂 目錄結構

```
blog-site/
├── content/
│   ├── posts/          ← 在這裡建立文章
│   ├── search/         ← 搜尋頁面
│   └── archives/       ← 歸檔頁面
├── static/             ← 放置圖片、CSS、JS 等靜態資源
├── themes/             ← 主題目錄
├── hugo.toml           ← 網站配置文件
└── start-server.ps1    ← 啟動腳本
```

## 🖼️ 添加圖片

### 方法一: 使用 static 目錄

1. 將圖片放在 `static/images/` 目錄
2. 在文章中引用:
```markdown
![圖片描述](/images/my-image.jpg)
```

### 方法二: 使用 Page Bundle

1. 建立與文章同名的目錄
2. 將文章重命名為 `index.md`
3. 圖片放在同一目錄
4. 引用:
```markdown
![圖片描述](my-image.jpg)
```

## 💡 寫作技巧

### Markdown 語法

```markdown
# H1 標題
## H2 標題
### H3 標題

**粗體**
*斜體*
~~刪除線~~

- 無序列表
1. 有序列表

[連結](https://example.com)
![圖片](image.jpg)

> 引用

`程式碼`

​```python
# 程式碼區塊
print("Hello")
​```
```

### Front Matter 完整範例

```yaml
---
title: "完整的文章範例"
date: 2025-11-20T16:00:00+08:00
lastmod: 2025-11-20T18:00:00+08:00
draft: false
author: "作者名稱"
description: "文章的簡短描述,用於 SEO"
tags: ["Hugo", "部落格", "教學"]
categories: ["技術"]
series: ["Hugo 系列"]
cover:
    image: "cover.jpg"
    alt: "封面圖片描述"
    caption: "封面圖片說明"
ShowToc: true
TocOpen: false
---
```

## 🚀 部署到 GitHub Pages

詳細步驟請參考 `DEPLOYMENT.md` 文件。

簡要步驟:
1. 建立 GitHub repository
2. 更新 `hugo.toml` 中的 `baseURL`
3. 推送程式碼到 GitHub
4. 在 GitHub Settings > Pages 中選擇 "GitHub Actions"
5. 等待自動部署完成

## ❓ 需要幫助?

- 📖 查看 `README.md` - 完整的專案說明
- 🚀 查看 `DEPLOYMENT.md` - 詳細的部署指南
- 🌐 訪問 [Hugo 官方文件](https://gohugo.io/documentation/)
- 🎨 訪問 [PaperMod 主題文件](https://github.com/adityatelange/hugo-PaperMod/wiki)

---

**開始你的寫作之旅吧!** ✨
